name: Build test

on:
  push:
    branches: [main]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      # 1. IL MANQUAIT CETTE ÉTAPE : Télécharger ton code sur la machine
      - name: Checkout du code
        uses: actions/chec  kout@v4

      # 2. Ta commande d'origine
      - name: Build de l'image Docker
        run: docker build -t nodejs_postgres-node:latest .